# Development Memory Log

## Iteration 1 - 2026-04-07
**Work Done:**
- Initialized Node.js project.
- Installed required dependencies: `express`, `cors`, `dotenv`, `@google/generative-ai`.
- Updated `package.json` to use "type": "module" for ES module support.
- Created `server.js` backend with:
  - Express server setup to serve static files.
  - Smart logic layer with `calculateHealthScore` and `generateBehaviorInsight` functions to compute context before calling AI.
  - Endpoint `/api/coach` configured to prompt the Gemini 2.5 Flash model and return a JSON payload with meal suggestion, reason, health score, tip, and swap suggestion.
- Created frontend in the `public` directory:
  - `index.html`: Clean and modern UI with goal, last meal, time, and mood inputs.
  - `style.css`: Minimalist premium Vanilla CSS design featuring dark mode, glassmorphism cards, dynamic glowing background mesh, and gradient text.
  - `app.js`: Client-side logic for connecting the UI to the backend endpoint, fetching JSON generated by Gemini, and updating the UI correctly.
- Application logic has been thoroughly mapped to the requested guidelines (Behavior-insight tracking, Swap suggestions, Health Score rule-engine).
- Prepared `README.md` with full context, assumptions, logic details, and instructions to deploy using Google Cloud Run under the limit.
